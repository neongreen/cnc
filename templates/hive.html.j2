<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Hive</title>
  <style>
    @font-face {
      font-family: 'MonaspaceKrypton';
      src: url('https://cdn.jsdelivr.net/gh/githubnext/monaspace@v1.000/fonts/webfonts/MonaspaceKrypton-Regular.woff') format('woff');
      font-weight: normal;
      font-style: normal;
    }

    @font-face {
      font-family: 'MonaspaceRadon';
      src: url('https://cdn.jsdelivr.net/gh/githubnext/monaspace@v1.000/fonts/webfonts/MonaspaceRadon-Regular.woff') format('woff');
      font-weight: normal;
      font-style: normal;
    }

    html {
      font-size: 14px;
    }

    body {
      font-family: sans-serif;
      margin: 20px;
    }

    table {
      width: auto;
      border-collapse: collapse;
      margin-top: 20px;
    }

    th,
    td {
      border: 1px solid #ddd;
      padding: 0.25rem 0.5rem;
      text-align: center;
      min-width: 4rem;
    }

    th {
      background-color: #f2f2f2;
      font-weight: bold;
      font-size: 1rem;
    }

    .date-text {
      color: gray;
      font-size: 0.75rem;
    }

    .score-text {
      font-size: 1rem;
    }

    .win-cell {
      background-color: #d4edda;
    }

    .loss-cell {
      background-color: #f8d7da;
    }

    .self-match-cell {
      background-color: #ddd;
    }

    .pear-link {
      text-decoration: none;
      color: inherit;
      border-bottom: 1px dashed rgba(0, 0, 0, 0.2);
    }

    .matchup-table {
      margin-top: 20px;
      margin-bottom: 40px;
    }

    .matchup-table th {
      font-weight: bold;
    }

    .matchup-table th,
    .matchup-table td {
      border: 1px solid #ddd;
      padding: 0.5rem;
      text-align: center;
    }

    .matchup-table .self-match {
      background-color: #ddd;
      color: #666;
    }

    .matchup-table .no-matches {
      background-color: #f8f9fa;
      color: #6c757d;
    }

    .matchup-table .has-matches {
      background-color: #eff8ff;
    }

    .matchup-table .highlighted {
      background-color: #fff197;
    }

    .matchup-table .has-matches {
      font-family: 'MonaspaceRadon', monospace;
    }

    .player-link {
      color: #1a73e8;
      text-decoration: none;
      font-weight: 700;
      transition: color 0.2s;
    }

    .player-link:hover {
      color: #0056b3;
      text-decoration: underline;
    }

    .player-link:visited {
      color: #6f42c1;
    }
  </style>
</head>

<body>
  <h1>Hive</h1>
  <p>
    Source on GitHub:
    <a href="https://github.com/neongreen/cnc" target="_blank">neongreen/cnc</a>
  </p>

  <h2>Game counts between players</h2>
  <p>
    This table shows the number of games played between each pair of players.
    The first row shows wins-losses-draws for rated games, the second row — for unrated games.
  </p>
  {{ game_counts_table | safe }}
</body>

</html>