---
description:
globs:
alwaysApply: true
---

Never create py files outside the src/ folder.

Don't run `mise build` every time you change something.
Typecheck is enough.

After you think the task is done, run `mise check` to typecheck everything.
